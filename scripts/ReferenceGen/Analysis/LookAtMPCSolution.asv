clear, clc, close all;
%% mpc logs
mpc_soln = readmatrix("../../../HKDMPC/log/state_log.txt");
time = 0:0.01:0.01*(size(mpc_soln(:,1))-1);

%% reference
name = 'AngledBoxRight';
Gait = "../PreProcessed/MIP_Hopping/"+name+"/";

body_states = readmatrix(Gait + "body_state.csv");
body_states(:,1:3) = flip(body_states(:,1:3),2);

plot(time,mpc_soln(:,3))
plot(time(body_states(1:length(time))))